import {ViewStyle} from 'react-native';

export const $flexRowStyle: ViewStyle = {
  flexDirection: 'row',
  justifyContent: 'space-between',
  alignItems: 'center',
};

export const $flexColumnStyle: ViewStyle = {
  flexDirection: 'column',
};
